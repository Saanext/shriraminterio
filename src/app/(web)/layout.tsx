
// This file is intentionally left blank. 
// The layout logic has been moved to src/components/site-layout.tsx and src/app/layout.tsx to resolve a server-rendering issue.
export default function WebLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
